---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumbs from '../Breadcrumbs.astro';
import FaqBlock from '../FaqBlock.astro';
import RelatedLinks from '../RelatedLinks.astro';

export interface Props { page: any; allPages: any[]; }
const { page, allPages } = Astro.props;
---

<BaseLayout title={page.title} description={page.meta} hub={page.hub} url={page.url}>
  <Breadcrumbs hub={page.hub} title={page.title} />
  <article class="max-w-3xl mx-auto">
    <h1>{page.title}</h1>
    <p class="text-xl text-ash mt-4 mb-8">{page.meta}</p>
    <div class="prose prose-invert prose-lg max-w-none">
      <slot />
      <p>This is the core content area for {page.title}. The recall thread design ensures every piece of information feels connected to your persistent memory.</p>
    </div>
    <RelatedLinks currentHub={page.hub} allPages={allPages} />
    <FaqBlock 
      pageUrl={page.url} 
      faqs={[
        { q: "Is this free to use?", a: "Yes, PersonalAI.cloud offers a generous free tier with persistent memory." },
        { q: "How does the memory work?", a: "We use advanced vector databases to store your preferences securely." }
      ]} 
    />
  </article>
</BaseLayout>
